services:
  gateway:
    build: .
    command: node packages/gateway/dist/index.js
    ports:
      - "4000:4000"
    environment:
      - PORT=4000
    volumes:
      - ./.consilium:/app/.consilium

